image: node:18

pipelines:
  default:
    - step:
        name: Build & Deploy Next.js to Railway
        caches:
          - node
        script:
          - npm install --legacy-peer-deps
          - npm run build
          - npm install -g @railway/cli
          - railway up --service $RAILWAY_SERVICE
